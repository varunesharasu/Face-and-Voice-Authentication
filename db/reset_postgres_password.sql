-- This SQL command resets the password for the PostgreSQL user 'postgres' to 'biometric123'.
-- Make sure to run this command with appropriate privileges.
ALTER USER postgres WITH PASSWORD 'biometric123';
